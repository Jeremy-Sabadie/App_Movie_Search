 <h1>RECHERCHER UN FILM</h1>
  <p>Entrée le titre du film</p>

	 <%= form_tag(movies_search_path, method: "get") do |f| %>

	      <%= label_tag :Search %>
	      <%= text_field_tag :usersearch%>

	      <%= submit_tag :Submit, :class =>"w3-btn w3-white"%>
<% end %>

<% unless @my_res == nil %>

  <% @my_res.each do |result| %>
     <br><br><h3><%= result["title"] %></h3>
     <p> Date de sortie: <%= result["release_date"] %></p>
     <img src = "http://image.tmdb.org/t/p/w185/<%= result["poster_path"]%>" id = "poster"><br><br><br>
  <% end %>
<% end %>